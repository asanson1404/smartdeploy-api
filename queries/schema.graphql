type Query {
    eventByTopic: EventConnection
}

type EventConnection {
    edges: [EventEdge]
}

type EventEdge {
    node: Event
}

type Event {
    contractId: String
    data: String
    ledger: Int
    ledgerTimestamp: Int
    topic1: String
    topic2: String
    topic3: String
    topic4: String
}